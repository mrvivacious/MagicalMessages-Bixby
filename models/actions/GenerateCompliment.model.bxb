action (GenerateCompliment) {
  // Specify this action as a calculation (the other types don't compile lol)
  type (Calculation) 

  // Set the output of this action to "MessageConcept"
  output (MessageResultConcept)
}
